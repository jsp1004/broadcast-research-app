# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 현황

이 저장소는 아직 코드가 스캐폴딩되지 않은 **기획 단계**입니다. 현재 존재하는 파일은 기획서(`PRD.md`, `prd_lite.md`)와 `.env`, `.gitignore`뿐입니다. Next.js 프로젝트 초기화부터 시작해야 하며, 빌드/린트/테스트 명령은 스캐폴딩 이후 이 파일에 추가해야 합니다.

기능 상세나 화면 흐름을 확인할 때는 이 파일보다 [`PRD.md`](PRD.md)(전체 기획서)와 [`prd_lite.md`](prd_lite.md)(요약본)를 우선 참고하세요.

## 한 줄 요약

방송 프로그램 기획자/PD가 장르·출연자를 입력하면, 웹(뉴스·커뮤니티·유튜브 댓글·SNS)에서 유사 프로그램 사례와 출연자 평판을 AI가 자동 수집해 정리해주는 리서치 도구.

## 기술 스택 (예정)

- **Next.js** — 프론트엔드와 API를 한 프로젝트에서 처리 (3일차 Vercel 배포까지 이어지도록 고정)
- **Supabase** — 검색 결과 저장용 데이터베이스
- **OpenAI API** — 웹 검색 기반 리서치 결과 생성
- **Vercel** — 배포 (로그인 없는 공개 URL, 별도 접근 제한 없음)

`.env`에 `OPENAI_API_KEY`, `SUPABASE_ACCESS_TOKEN`, `VERCEL_TOKEN`, `GITHUB_TOKEN`이 정의되어 있으며 `.gitignore`에 등록되어 있습니다. 값을 로그, 커밋, 코드에 노출하지 마세요.

## 핵심 기능과 AI 준수 규칙

기능을 구현할 때 아래 규칙은 프롬프트/로직 설계에 그대로 반영되어야 합니다 (임의로 완화하지 말 것).

### 1) 유사 프로그램 리서치
장르/키워드 입력 → 국내외 유사 프로그램을 카드(포맷·화제성·성공 여부·출처)로 정리.
- 결과 5~10개로 제한. 5개 미만이면 "관련 프로그램 ○건 확인됨"으로 안내
- 국내·해외 각 최소 1개 이상 포함
- 프로그램마다 출처 링크 필수 표기
- 신뢰 가능한 출처 우선, 불분명한 정보는 "미확인 정보"로 별도 표시
- 확인 안 되는 정보는 추측 금지 → "정보 없음" (프로그램 자체 미발견 vs 일부 항목 누락을 구분해서 안내)
- 검색 결과가 전혀 없으면 "유사 프로그램을 찾지 못했습니다" + 검색어 조정 안내

### 2) 예상 출연자 리서치
출연자명 입력 → 이전 출연작·화제성·호감도(%)를 카드로 정리.
- 최근 36개월 이내 자료 우선 수집
- 긍정/부정 언급 비율을 수치(%)로 제시
- 서로 다른 출처 3곳 이상 교차 확인 후 정리
- 논란·이슈성 정보는 출처와 함께 별도 구분 표시
- 동명이인 발견 시 임의로 특정하지 말고 구분 정보(소속사·활동 분야 등)와 함께 사용자가 선택하게 함

### 공통 규칙
- 화제성 기준(최근 3개월 언급량)은 검색 시점마다 매번 자동 재계산
- 데이터 수집 실패 시 최대 2회 자동 재시도 → 그래도 실패하면 오류 메시지 + 재시도 버튼, 부분 결과라도 있으면 표시
- 해외 자료는 원문과 한국어 번역을 함께 제시

## 범위 밖 (구현하지 말 것)

- 출연자 개인 연락처 수집/직접 컨택 기능
- 기획안·대본 AI 자동 작성
- 시청률 예측 등 AI 예측 모델
- SNS 실시간 추적/알림
- 팀 협업·댓글·권한 관리
- 로그인/계정 시스템 (누구나 URL로 바로 접근)
- API 사용량 제한/과금 관리 (추후 검토 대상)

## 보안/데이터 관련 참고

- 다루는 데이터는 전부 공개된 웹 자료(기사, 커뮤니티, SNS)이며 비공개 개인정보(연락처 등)는 수집하지 않음
- API 키는 `.env`로만 관리, 저장소에 커밋 금지

## Claude Code 작업 규칙

- 모든 설명과 코드 주석은 한국어로 작성한다.
- 새 파일은 `my-app` 폴더 안에만 만든다.
- 기술 스택은 PRD에 정한 대로 Next.js로 고정한다. 다른 프레임워크로 바꾸거나 마이그레이션을 제안하지 않는다. 배포는 Vercel을 사용한다.
- 코드를 바꾸면 반드시 무엇을 왜 바꿨는지 한 줄로 알려준다.
- `.env` 등 비밀 정보 파일과 `node_modules` 폴더는 `.gitignore`에 등록해 두고, 절대 커밋하지 않는다.
- 외부 서비스 인증이 필요하면 토큰 값을 사용자에게 묻거나 채팅에 출력하지 말고, `.env`에 있는 값을 읽어서 사용한다.
  - 예: Supabase를 쓸 상황이 생기면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업한다.
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증해 작업한다.
- 파일을 지워야 할 때는 바로 삭제하지 말고, `trash-can` 폴더를 만들어 그 안으로 옮겨만 둔다. 작업이 끝난 뒤 사용자가 직접 확인하고 삭제한다.
- 이미 설치된 서브에이전트는 필요할 때마다 적극 활용한다.
