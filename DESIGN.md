# DESIGN.md

PLAN.md와 PRD.md를 바탕으로 한 화면·데이터·기술 설계입니다.

## 1. 화면 구성 (무엇이 어디에 보이는지)

### 메인 화면 (`/`)
- 상단: 서비스 제목/한 줄 소개
- 검색 영역 (2개 섹션 나란히 또는 탭으로 구분)
  - **유사 프로그램 검색**: 장르/키워드 입력창 + 검색 버튼
  - **예상 출연자 리서치**: 출연자명 입력창 + 검색 버튼

### 유사 프로그램 결과 영역
검색 후 같은 화면 하단에 카드 리스트로 표시. 카드 1개 = 프로그램 1개:
- 프로그램명 + 국내/해외 배지 — 국내 프로그램명은 한글 정식 명칭, 해외 프로그램명은 영어 명칭으로 표기
- 포맷 설명, 화제성 지표(산출 근거: 최근 3개월 기사·SNS 게시물·댓글 수), 성공 여부(시청률 예측이 아니라 과거 시청률·화제성 지표·수상 기록 등 이미 확정된 데이터에 기반한 판단 근거 포함)
- 출처 링크 (필수). 유튜브 데이터로 화제성을 보강한 경우, 실제 유튜브 영상 링크도 별도 "유튜브 출처"로 볼 수 있게 표시
- 정보가 불분명하면 "미확인 정보" 표시 배지
- 특정 항목 값을 찾지 못하면 "정보 없음"으로 표시 — 프로그램 자체를 못 찾은 경우(카드 없음, 아래 "결과 없음 상태" 참고)와는 구분되는 별도 상태
- 해외 프로그램은 원문 요약과 한국어 번역을 함께 표시
- 검색 결과가 5건 미만이면 카드 리스트 상단에 "관련 프로그램 ○건 확인됨" 안내 배너 표시

### 예상 출연자 결과 영역
검색 후 인물 1명당 종합 요약 카드 1개로 표시 (출연작마다 낱개 카드를 만들지 않고, 카드 안에 출연작 목록을 하위 항목으로 포함). 출연자명은 한글 이름으로 표기:
- 출연작 목록, 화제성 지표(산출 근거: 최근 3개월 기사·SNS 게시물·댓글 수)
- 호감도(긍정/부정 %) — 막대 그래프 형태, 산출 근거(수집한 댓글·게시물 수)를 함께 표기. 유튜브 데이터로 보강한 경우, 실제 유튜브 영상 링크도 별도 "유튜브 출처"로 볼 수 있게 표시
- 논란·이슈성 정보는 별도 색상/구분으로 표시
- 서로 다른 출처 3곳 이상을 실제로 교차 확인한 뒤 정리된 결과만 표시 (단순히 링크 3개를 나열하는 것이 아니라, 교차 확인 로직을 통과한 결과만 노출)
- 신뢰 가능한 출처를 우선하고, 출처가 불분명한 정보는 "미확인 정보" 배지로 별도 표시
- 해외 자료는 원문 요약과 한국어 번역을 함께 표시

### 동명이인 선택 화면 (검색 직후 조건부 표시)
동명이인이 여러 명 검색되면 결과 카드 대신 후보 리스트를 먼저 보여줌 (이름 + 소속사·활동 분야) → 사용자가 하나를 클릭해야 리서치가 진행됨

### 로딩 / 결과 없음 / 오류 상태
- 검색 중: 로딩 표시
- 검색어에 해당하는 프로그램·출연자를 아예 찾지 못한 경우: "유사 프로그램을 찾지 못했습니다" 등 결과 없음 안내 + 검색어 조정 제안 (카드 자체가 없는 상태로, 카드 안의 "정보 없음"과는 다른 화면)
- 수집 실패(최대 2회 자동 재시도 후에도 실패): 오류 메시지 + "다시 시도" 버튼, 이미 모인 부분 결과가 있으면 함께 표시

### 검색 결과 재열람
로그인 없는 공개 URL 서비스라 다른 사용자의 검색어(기획 아이디어, 검토 중인 출연자명)가 노출되면 안 되므로, 모든 검색 기록을 나열하는 공개 목록 화면은 두지 않음. 대신 검색 완료 시 결과별 고유 링크(`/result/{id}`)가 발급되고, 그 링크를 아는 사람만 해당 결과를 다시 열람할 수 있음(목록에서 찾아볼 수 없음). 재열람 화면에는 "○○ 시각 기준 저장된 결과"라는 시점 안내를 표시해, 화제성 수치가 최신이 아닐 수 있음을 알림

## 2. 데이터 흐름 (입력 → 처리 → 출력)

1. **입력**: 사용자가 메인 화면에서 장르/키워드 또는 출연자명을 입력하고 검색 버튼 클릭
2. **요청**: 브라우저가 Next.js API Route(`/api/research/programs`, `/api/research/cast`)로 검색어 전송
   - 출연자 리서치에서 동명이인이 여러 명이면 먼저 `/api/research/cast/candidates`로 후보 목록(이름 + 소속사·활동 분야)만 조회해 사용자에게 보여주고, 사용자가 후보를 선택한 뒤에야 `/api/research/cast`를 호출해 실제 리서치를 진행 (2단계 흐름)
3. **AI 리서치**: API Route가 OpenAI API(웹 검색 기능)를 호출해 뉴스·커뮤니티·SNS(인스타그램, X 등) 게시물을 조회. 이때 PRD의 규칙(결과 개수 제한, 출처 필수, 신뢰 출처 우선, 미확인 정보 표시, 최근 기간 우선, 호감도 산출 근거 확보 등)을 함께 지시문으로 전달
   - 유튜브 댓글은 OpenAI 웹 검색만으로는 개별 댓글 단위 데이터를 안정적으로 얻기 어려워, YouTube Data API를 별도로 호출해 관련 영상의 댓글·좋아요 수를 직접 가져오고, 이 수치를 화제성 지표와 호감도(긍정/부정 %) 산출 근거에 반영
4. **검증**: 서버에서 AI 응답을 파싱하고 PRD 규칙을 만족하는지 확인 — 카드 개수, 국내·해외 포함 여부, 출처가 실제로 존재하는지와 서로 다른 출처 3곳을 교차 확인했는지, "정보 없음"(일부 항목 누락)과 "결과 없음"(전체 미발견)의 구분, 화제성 산출 기준(최근 3개월)과 출연자 자료 수집 범위(최근 36개월)를 혼동하지 않았는지, 동명이인 여부 등
5. **저장**: 검증된 결과를 Supabase 테이블에 저장 (스키마는 3장 참고). 수집 실패로 부분 결과만 얻었을 경우 부분 결과임을 표시해 저장
6. **응답**: 서버가 정리된 결과를 JSON으로 브라우저에 전달
7. **출력**: 브라우저가 결과를 카드 UI로 렌더링 (또는 동명이인 선택 화면 / 오류 화면으로 분기)
8. **실패 처리**: 수집 실패 시 서버에서 최대 2회 자동 재시도 → 그래도 실패하면 오류 메시지와 재시도 버튼을 화면에 표시, 부분 결과가 있으면 함께 노출

## 3. 데이터 저장 구조 (Supabase 스키마 초안)

### `research_results` 테이블
| 컬럼 | 설명 |
|---|---|
| id | 결과 고유 ID (재열람 링크 `/result/{id}`에 사용) |
| type | `program` \| `cast` — 유사 프로그램 리서치인지 예상 출연자 리서치인지 구분 |
| query | 사용자가 입력한 검색어(장르/키워드 또는 출연자명) |
| result_data | 카드 목록 전체(JSON) — 프로그램/출연자 카드, 원문+번역, 미확인 정보 여부 포함 |
| is_partial | 수집 실패로 인한 부분 결과 여부 |
| selected_person | 동명이인 중 사용자가 선택한 인물의 구분 정보 (해당하는 경우만) |
| searched_at | 검색 시각 (재열람 시 "○○ 시각 기준" 안내 및 화제성 재계산 여부 판단에 사용) |

## 4. 기술 선택

### 기본 (PRD 고정)
- **Next.js** — 화면(프론트엔드)과 서버 API를 한 프로젝트 안에서 함께 개발. 마지막 날 Vercel 배포까지 그대로 이어짐
- **Vercel** — 완성된 코드를 인터넷에 공개 URL로 올려주는 배포 서비스
- **Supabase** — 검색 결과를 저장해두는 클라우드 데이터베이스
- **OpenAI API** — 인터넷 자료를 찾아 리서치 내용을 정리해주는 AI 엔진
- **YouTube Data API** — 유튜브 영상의 실제 댓글·좋아요 수를 가져오는 구글 API. OpenAI 웹 검색만으로는 개별 댓글 데이터를 안정적으로 얻기 어려워 추가로 결정됨(작업 5·8). `.env`의 `YOUTUBE_API_KEY`로 인증

### 추가로 제안하는 기술 (선택 사항)

| 기술 | 쉬운 설명 |
|---|---|
| Tailwind CSS | 화면의 색상·간격·정렬 같은 디자인을 코드로 빠르게 입히는 도구예요. 디자이너 없이도 깔끔한 화면을 만들 수 있게 도와줘요. |
| shadcn/ui | 버튼, 카드, 팝업 같은 화면 부품을 미리 만들어둔 조립 키트예요. 매번 처음부터 디자인하지 않아도 돼요. |
| Zod | AI가 준 답변이나 사용자 입력이 정해진 형식(카드 5~10개, 출처 링크 포함 등)에 맞는지 자동으로 검사해주는 도구예요. PRD 규칙을 실수로 어기는 걸 막아줘요. |
| Vercel AI SDK | OpenAI 같은 AI 서비스에 요청을 보내고 답을 받는 과정을 더 간단하게 만들어주는 도구예요. 개발 속도를 높여줘요. |

이 4가지는 필수가 아니라 개발 속도와 완성도를 높이기 위한 선택 사항이며, 최종 스택은 여전히 Next.js + Vercel + Supabase + OpenAI API를 기본 골격으로 합니다. 적용 시점은 PLAN.md 기준으로 Tailwind CSS·shadcn/ui는 작업 1(프로젝트 초기화), Zod는 작업 7·10(규칙 검증), Vercel AI SDK는 작업 5·8(OpenAI 연동)입니다.

## 5. 비주얼 스타일 가이드

- 메인 색상: 네이비/딥블루 계열 (신뢰감 있는 방송·미디어 업계 톤)
- 배경/카드: 흰색·연회색 배경에 카드는 옅은 그림자로 구분, 강조 요소(경고·배지)는 네이비와 대비되는 포인트 컬러 1개만 사용
- 레이아웃: 데스크톱 중심(업무용 PC 화면 기준), 모바일 대응은 이번 범위에서 필수 아님
- 전체 톤: 정보량이 많으므로 카드 사이 여백을 넉넉히 두어 "깔끔하고 정리된 리서치 대시보드" 느낌을 유지
